import { ConditionalDirective } from './conditional.directive';

describe('ConditionalDirective', () => {
  it('should create an instance', () => {
    const directive = new ConditionalDirective();
    expect(directive).toBeTruthy();
  });
});
